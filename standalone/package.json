{
  "name": "pixel-agents-standalone",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "description": "Standalone browser server for Pixel Agents backed by Codex sessions.",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/codex-scanner.js && node --check src/codex-session-parser.js && node --check src/assets.js && node --check src/hooks.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "chokidar": "^4.0.3",
    "express": "^4.21.2",
    "pngjs": "^7.0.0",
    "ws": "^8.18.3"
  }
}
